# CircuitMentor — Python Dependencies
# Install: pip install -r requirements.txt
#
# Note: PyTorch version depends on your CUDA version.
# For Jetson AGX Orin (CUDA 12.6): use the JetPack-provided PyTorch.
# For discrete GPU: pip install torch==2.1.0 --index-url https://download.pytorch.org/whl/cu121

# ── Core ML ──────────────────────────────────────────────────────────────────
transformers==4.44.0
peft==0.18.1
trl==0.8.6
datasets==2.20.0
accelerate==0.33.0
safetensors==0.4.3
tokenizers==0.19.1
huggingface-hub==0.24.5
sentencepiece==0.2.0

# ── Dataset Builder ───────────────────────────────────────────────────────────
tqdm==4.66.4
colorama==0.4.6

# ── Backend (FastAPI) ─────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.4
python-multipart==0.0.9
httpx==0.27.0
requests==2.32.3

# ── Frontend (Streamlit) ──────────────────────────────────────────────────────
streamlit==1.36.0

# ── Tunneling ─────────────────────────────────────────────────────────────────
pyngrok==7.1.6

# ── System monitoring (admin dashboard) ──────────────────────────────────────
psutil==6.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy==1.26.4
packaging==24.1

# ── NOT included (install separately based on hardware) ───────────────────────
# torch         — install via JetPack (Jetson) or pytorch.org (discrete GPU)
# bitsandbytes  — NOT compatible with CUDA 12.6 on Jetson ARM64
#                 For discrete GPU: pip install bitsandbytes (enables QLoRA)
